import { Router } from "express"; import { asyncHandler } from "../../core/asyncHandler.js"; import { requireAdmin,requireAuth } from "../../middleware/auth.js"; import { validate } from "../../middleware/validate.js"; import * as controller from "./skills.controller.js"; import { idValidation,skillValidation } from "./skills.validation.js";
const router=Router(); router.get("/",asyncHandler(controller.list)); router.use(requireAuth,requireAdmin); router.post("/",skillValidation,validate,asyncHandler(controller.create)); router.put("/:id",[...idValidation,...skillValidation],validate,asyncHandler(controller.update)); router.delete("/:id",idValidation,validate,asyncHandler(controller.remove)); export default router;
