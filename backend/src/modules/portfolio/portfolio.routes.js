import { Router } from "express"; import { asyncHandler } from "../../core/asyncHandler.js"; import { requireAdmin,requireAuth } from "../../middleware/auth.js"; import * as controller from "./portfolio.controller.js"; const router=Router();router.get("/public",asyncHandler(controller.publicPortfolio));router.get("/export",requireAuth,requireAdmin,asyncHandler(controller.exportData));router.post("/import",requireAuth,requireAdmin,asyncHandler(controller.importData));export default router;
