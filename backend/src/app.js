import express from "express"; import cors from "cors"; import helmet from "helmet"; import cookieParser from "cookie-parser"; import morgan from "morgan"; import { env } from "./config/env.js"; import { requestId } from "./middleware/requestId.js"; import { globalLimiter } from "./middleware/rateLimiters.js"; import { originGuard } from "./middleware/originGuard.js"; import { notFound } from "./middleware/notFound.js"; import { errorHandler } from "./middleware/errorHandler.js"; import auditRoutes from "./modules/audit/audit.routes.js"; import authRoutes from "./modules/auth/auth.routes.js"; import profileRoutes from "./modules/profile/profile.routes.js"; import skillsRoutes from "./modules/skills/skills.routes.js"; import projectsRoutes from "./modules/projects/projects.routes.js"; import contactRoutes from "./modules/contact/contact.routes.js"; import portfolioRoutes from "./modules/portfolio/portfolio.routes.js"; import cvRoutes from "./modules/cv/cv.routes.js";
const app=express();app.disable("x-powered-by");app.set("trust proxy",1);app.use(requestId);app.use(helmet({crossOriginResourcePolicy:{policy:"cross-origin"}}));app.use(cors({origin(origin,callback){if(!origin||env.allowedOrigins.includes(origin))return callback(null,true);callback(null,false)},credentials:true,methods:["GET","POST","PUT","PATCH","DELETE","OPTIONS"]}));app.use(cookieParser());app.use(express.json({limit:"750kb"}));app.use(express.urlencoded({extended:false,limit:"100kb"}));if(!env.isProduction)app.use(morgan("tiny"));app.use(globalLimiter);app.use(originGuard);app.get("/api/health",(req,res)=>res.json({status:"ok"}));app.use("/api/audit",auditRoutes);app.use("/api/auth",authRoutes);app.use("/api/profile",profileRoutes);app.use("/api/skills",skillsRoutes);app.use("/api/projects",projectsRoutes);app.use("/api/contact-requests",contactRoutes);app.use("/api/portfolio",portfolioRoutes);app.use("/api/cv",cvRoutes);app.use(notFound);app.use(errorHandler);export default app;
